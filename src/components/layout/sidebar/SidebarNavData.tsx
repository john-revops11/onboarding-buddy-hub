
import { IconComponentMap } from "./icons";
import { adminNavGroups } from "./admin-nav-data";
import { userNavGroups } from "./user-nav-data";
import { SidebarNavGroup, SidebarNavItem } from "./types";

// Re-export for backward compatibility
export { adminNavGroups, userNavGroups };
export type { SidebarNavGroup, SidebarNavItem };

// Export the icon component map for use in components
export { IconComponentMap };
